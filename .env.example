# Frontend Environment Variables
# Backend API URL - configure this to point to your backend server
# If not set, the frontend will try to connect to the same hostname on port 5000
VITE_API_URL=http://localhost:5000/api
