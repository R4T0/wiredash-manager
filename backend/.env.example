# Configurações do Flask
FLASK_ENV=development
FLASK_DEBUG=True

# URL base da aplicação (usado para links de recuperação de senha)
APP_URL=http://localhost:3000

# Configurações de CORS (opcional)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://0.0.0.0:3000

# ==========================================
# Variáveis do Frontend (usadas no build)
# ==========================================
# URL da API do backend que o frontend irá utilizar
# Se não definida, o frontend tentará conectar no mesmo hostname na porta 5000
# VITE_API_URL=http://localhost:5000/api
